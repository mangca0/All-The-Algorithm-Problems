单调性
# set+双指针 高效遍历 无重复字符的子串
```
while(r < n)
{
	while(set中有str[r]):
		set.erase(str[l])
		l ++
	set.insert(str[r])
	```
	```
	r ++
}
```

lanqiao 20096 开赛主题曲【算法赛】

# 滑动窗口
开for枚举r端点，配合l指针++ **有优越性**

# n^3遍历子矩阵和
[[蓝桥2109 统计子矩阵]]
矩阵没有负值，前缀和有单调性
n^2确定上下界，n双指针

⬇️ 统计有多少子矩阵和不超过k
```cpp
for(int up = 1; up <= n; up ++)
	for(int down = up; down <= n; down ++)
	{ 
		int l = 1, sum = 0; 
		for(int r = 1; r <= m; r ++) 
		{ 
			sum += pre[down][r] - pre[up - 1][r];
			while(sum > K && l <= r)
			{ 
				sum -= pre[down][l] - pre[up - 1][l];
				l ++;
			}
		ans += r - l + 1;
		}
	}
```