# strtol
字符串转数字
long strtol(const char * restrict nptr, char ** restrict endptr, int base);
**用法：strtol(str, &end, 10)**
	endptr是一个指针的地址，**char * end;即可**
	**base为进制**

同类型还有：strtoll、strtoull
简单版：atoi、atol、atoll（无错误检查）
c++：stoi