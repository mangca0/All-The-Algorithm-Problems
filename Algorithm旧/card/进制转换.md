**用是否reverse记忆，string->int->string**
# -> 10
```
for(auto i : s)
{
	int tmp;
	if(isdigit(i))tmp = i - '0';
	else tmp = i - 'A' + 10;
	t = t * from + tmp;
}
```

# 10 ->
```
while(t)
{
	int tmp = t % to;
	if(tmp <= 9)tmp = tmp + '0';
	else tmp = tmp + 'A' - 10;
	s += tmp;
	t /= to;
}
reverse(s.begin(), s.end());
```


