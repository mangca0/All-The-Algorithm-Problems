**p必须为质数，若p不是质数，用[[线形递推求区间逆元]]**


# 费马小
当p为质数，且gcd(p, a) = 1， 有$a^{p - 1} \equiv 1 (mod p)$

# 逆元
若有$a \cdot b \equiv 1 (mod p)$，则称b是a的逆元，$b = a^{-1}，b = \frac{1}{a}$

$b = a^{p-2}$ % p
```
int inv(int, a)
{
	return qp(a, p - 2);
}
```
qp为[[快速幂]]函数

