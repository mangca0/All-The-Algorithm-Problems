贪心 + 排序

```cpp
v.push_back({l, r});
sort(v.begin(), v.end());

if((int)vv.size() == 0)return false;// 根据题目 或者没覆盖到1位置
L = v[0].first;
R = v[0].second;
for(const auto &i : v)
{
	if(i.first <= R + 1)// 如果相邻或相交
		R = max(R, i.second);
	else break;// 根据题目
}
```