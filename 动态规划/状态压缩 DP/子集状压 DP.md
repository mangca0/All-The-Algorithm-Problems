- **定义**：这是一种特殊的**状态转移方式**。它不仅用 `mask` 表示集合，而且在进行状态转移时，它不再是每次只加减一个元素，而是**直接枚举当前集合的所有子集**。
    
- **转移特征**：**“成块（块状）”的子集转移。**
    
    假设当前状态是 `mask`（代表子集 $S$），你需要找到 $S$ 的一个子集 $S'$（对应二进制数为 `sub`），让状态 $S$ 从 $S \setminus S'$ 转移而来。
    
- **核心代码（位运算黑魔法）**：
    
    在 C++ 或 Java 中，枚举一个二进制数 `mask` 的所有子集 `sub`，有一个非常经典的位运算公式：  
·