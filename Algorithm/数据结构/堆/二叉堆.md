**下列代码均以大根堆为例，swap方法请自行实现**

## 堆结构
### 基本操作
「完全二叉树」和数组前缀范围来对应，大小由单独变量size控制

heapInsert（向上调整）
```java
public static void heapInsert(int[] arr, int i) {
	while (arr[i] > arr[(i - 1) / 2]) {
		swap(arr, i, (i - 1) / 2);
		i = (i - 1) / 2;
	}
}
```
𝑂(log⁡𝑛)

heapify（向下调整）
```java
public static void heapify(int[] arr, int i, int size) {
	int l = i * 2 + 1;
	while (l < size) {
		int best = l + 1 < size && arr[l + 1] > arr[l] ? l + 1 : l;
		best = arr[best] > arr[i] ? best : i;
		if (best == i) break;
		swap(arr, best, i);
		i = best;
		l = i * 2 + 1;
	}
}
```
𝑂(log⁡𝑛)

### 建堆
#### 方法一：从顶到底建堆
每次都是一个向上调整（插入）
```java
public static void buildHeap1(int[] arr) {  
    for (int i = 0; i < arr.length; i++) {  
        heapInsert(arr, i);  
    }
}
```
提醒：对于第 𝑘 层的结点，向上调整的复杂度为 𝑂(𝑘)

每次向上调整的复杂度是 𝑂(log⁡𝑛)，每次插入堆规模加一
所以总复杂度：log⁡1 +log⁡2 + ⋯ +log⁡𝑛 = Θ(𝑛log⁡𝑛)，用斯特林公式证明或增倍分析法

#### 方法二：从底到顶建堆
每次都是一个向下调整，可以理解为已经有一个堆
```java
public static void buildHeap2(int[] arr) {  
    for (int i = arr.length - 1; i >= 0; i--) {  
        heapify(arr, i, arr.length);  
    }  
}
```
最下层只需要向下调整一次，倒二层需要向下调整两次，⋯
提醒：这里的最下层不只是深度最深的结点，是所有叶子结点，倒二层同理
总复杂度：n/2 \* 1 + n/4 \* 2 + n/8 \* 3 + ⋯ = 𝑂(𝑛)，用高中数学知识可证

## 堆排序
建好堆后，依次弹出堆顶元素即可
```java
public static void heapSort(int[] arr) {  
    int size = arr.length; // 堆大小  
    while (size > 1) {  
        swap(arr, 0, --size);  
        heapify(arr, 0, size);  
    }  
}
```
𝑂(𝑛log⁡𝑛)

## 习题
- [力扣 912. 排序数组](https://leetcode.cn/problems/sort-an-array/) 堆排序模版
- [洛谷 P1177 【模板】排序](https://www.luogu.com.cn/problem/P1177)
- [洛谷 P1801 黑匣子](https://www.luogu.com.cn/problem/P1801)

- [洛谷 P15967 采矿文明](https://www.luogu.com.cn/problem/P15967) 维护前 $k$ 大元素和

**第 $K$ 小/大**

部分题也可以用二分解决.

[力扣 2386. 找出数组的第 K 大和](https://leetcode.cn/problems/find-the-k-sum-of-an-array/) 
>  本质是把「枚举选哪个」写法的 `for` 循环去掉.
>  枚举 $j$ = 替换最后一个数.
>  往下递归 = 在末尾添加数字

#atom