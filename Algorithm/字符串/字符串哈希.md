常做一种比较字符串工具，避免比较字符串时O(n)的复杂度，可以将字符串处理为数字作为键
有时可以替代 kmp 子串匹配，降低思维复杂度

## 字符串转化为base进制的数字
关于字符串的哈希值，我们一般采用进制的思想，将字符串想象成一个 127 进制的数。那么，对于每一个长度为 𝑛 的字符串 𝑠，就有：
$x = s_0 \cdot 499^0 + s_0 \cdot 499^1 + \cdots + s_n \cdot 499^n$
**注意：字符应1开始编码，而不是0**。例如 a~z 对应 1~26

常用质数：433 499 599 1e9+7
经典：31 131 1313 131313（不推荐，出题可能被造碰撞）
**注意：字符串哈希不算是很优秀的算法，如果过不了题，质数不行就换**

## long类型自然溢出
相当于对 2^64 取模
c++ 中可以利用`unsigned long long` 的自然溢出，等价于取模操作
java 中可以利用 `long`，自然溢出，负数无所谓，一样用

## 单字符串转换为哈希值
```java
private static long hash(char[] s) {  
    long value = 0L;  
    for (int i = 0; i < s.length; i++) {  
        value = value * 499 + v(s[i]);  
    }  
    return value;  
}
```
[洛谷 P3370【模板】字符串哈希](https://www.luogu.com.cn/problem/P3370)

## 多项式哈希
### 单模哈希模板
```java
// 多项式字符串哈希（方便计算子串哈希值）
// 哈希函数 hash(s) = s[0] * base^(n-1) + s[1] * base^(n-2) + ... + s[n-2] * base + s[n-1]
final int MOD = 1_070_777_777;
final int BASE = (int) 8e8 + (int) (Math.random() * ((int) 1e8)); // 随机 base，防止 hack
int[] powBase = new int[n + 1]; // powBase[i] = base^i
int[] preHash = new int[n + 1]; // 前缀哈希值 preHash[i] = hash(target[0] 到 target[i-1])
powBase[0] = 1;
for (int i = 0; i < n; i++) {
	powBase[i + 1] = (int) ((long) powBase[i] * BASE % MOD);
	preHash[i + 1] = (int) (((long) preHash[i] * BASE + s[i]) % MOD); // 秦九韶算法计算多项式哈希
}
```

前缀和得到子串哈希：
```java
// 这里数组下标为 0~n-1
private int getHash(int l, int r) {
    if (l == 0)
        return preHash[r];
    return (int) (((preHash[r] - (long) preHash[l - 1] * powBase[r - l + 1]) % MOD + MOD) % MOD);
}
```

## 用字符串哈希比较两个子串的字典序大小
做法是二分长度，计算最长公共前缀，然后比较最长公共前缀的下一个字母。时间复杂度：O(logn)。
[洛谷 P3763 TJOI2017 DNA](https://www.luogu.com.cn/problem/P3763) 二分长度
[力扣 3722. 反转后字典序最小的字符串](https://leetcode.cn/problems/lexicographically-smallest-string-after-reverse/) 同洛谷P3763

## 改进
这种方法虽然思维、代码难度简单，但并不算很优秀的。通过构造数据可使得发生冲突
我们可以使用双哈希的方法：选取两个大质数 𝑎,𝑏 。当且仅当两个字符串的哈希值对 𝑎 和对 𝑏 取模都相等时，我们才认为这两个字符串相等。这样可以大大降低哈希冲突的概率

## 习题 
蓝桥 2047 斤斤计较的小Z
**本题kmp与Hash均可实现**

[[洛谷 P4391 [BOI2009] Radio Transmission 无线传输]]

- 模板（滚动哈希、前缀哈希）
	- [洛谷 P3370 【模板】字符串哈希](https://www.luogu.com.cn/problem/P3370)
	- [力扣 28. 找出字符串中第一个匹配项的下标](https://leetcode.cn/problems/find-the-index-of-the-first-occurrence-in-a-string/) 
	- [力扣 187. 重复的DNA序列](https://leetcode.cn/problems/repeated-dna-sequences/) 
	- [牛客 130226 D](https://ac.nowcoder.com/acm/contest/130226/D) 同力扣 187
	- [力扣 2156. 查找给定哈希值的子串](https://leetcode.cn/problems/find-substring-with-given-hash-value/) 倒序
- 进阶
	- [力扣 1316. 不同的循环子字符串](https://leetcode.cn/problems/distinct-echo-substrings/) 
	- [力扣 1297. 子串的最大出现次数](https://leetcode.cn/problems/maximum-number-of-occurrences-of-a-substring/) 挺有意思的脑经急转弯
	- [力扣 2261. 含最多 K 个可整除元素的子数组](https://leetcode.cn/problems/k-divisible-elements-subarrays/) 用哈希对子数组去重
	- [力扣 686. 重复叠加字符串匹配](https://leetcode.cn/problems/repeated-string-match/) 上下界分析

[洛谷 P3763 TJOI2017 DNA](https://www.luogu.com.cn/problem/P3763) 二分长度
[力扣 3722. 反转后字典序最小的字符串](https://leetcode.cn/problems/lexicographically-smallest-string-after-reverse/) 同洛谷P3763


#atom 